package com.google.android.gms.tagmanager;

interface av<T> {

    public enum a {
        NOT_AVAILABLE,
        IO_ERROR,
        SERVER_ERROR,
        SERVER_UNAVAILABLE_ERROR
    }

    void a();

    void a(a aVar);

    void a(T t);
}
