package com.google.android.gms.internal;

import java.util.concurrent.TimeUnit;

public interface akg<DP, DT> {
    double a(DP dp, int i);

    long a(DP dp, TimeUnit timeUnit);

    aki<DT> a();

    DT a(DP dp);

    int b(DP dp, int i);

    String b(DP dp);

    boolean c(DP dp, int i);
}
